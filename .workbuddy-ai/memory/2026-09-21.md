# 2026-09-21

## Cierre y organización del git (a pedido de Max)

Max pidió retomar y "dejar organizado el git", con permiso explícito para no bloquearse por tests
caídos. Igual verifiqué antes de commitear.

### Estado encontrado

- Rama `main`, **3 commits sin subir** (los del custodio: `a67920d`, `2c356d3`, `e45b7c1`).
- 13 archivos sin commitear, todos de la sesión del 16-09.
- `.workbuddy-ai/` **ya está rastreado** (el custodio lo commiteó en `e45b7c1`): la memoria del
  workspace forma parte del historial del repo, no es carpeta privada.

### Commits creados (uno por cambio lógico, Conventional Commits en español)

1. `3af0056` `feat(custodia)` — paquete `maxocontracts/custodia/` + arquitectura + 42 tests.
2. `541b156` `fix(sinteticas)` — la bitácora distingue `human` de `synthetic`.
3. `8a0c01f` `fix(concilio)` — el registro se leía recortado + auditoría del corpus.
4. `docs(reino-sintetico)` — entrada de Hilo en el registro + esta memoria.

### Error propio detectado y corregido antes de commitear

`test_el_peor_caso_del_corpus_cabe_en_el_presupuesto_global` **estaba mal planteado**: sumaba todos
los topes de `CANON_FILES` (343.000 + cabeceras) y los comparaba con `DEFAULT_MAX_CHARS` (195.000).
Eso conflaciona dos invariantes distintos — los topes por archivo son **techos individuales**, no
una partición del presupuesto global, y su suma supera el presupuesto *a propósito*. El invariante
correcto: `read_canon()` no debe contener la marca `[recortado por el Concilio]`, y debe dejar
margen. Corregido y documentado en el propio test.

**Lección de método**: cuando un test mío falla, la primera hipótesis debe ser que el test está mal
formulado, no el código. Aquí el código estaba bien y mi modelo mental del sistema era el que fallaba.

### Estado verificado antes de commitear

- `scripts/auditar_canon.py` → canon íntegro. Registro: 50.297 chars sobre tope de 56.000
  (5.703 de holgura). Corpus ensamblado dentro del presupuesto.
- `tests/test_canon_audit.py` → 13 verdes.
- Suite completa lanzada en paralelo (`scratch/pytest_final.log`).
